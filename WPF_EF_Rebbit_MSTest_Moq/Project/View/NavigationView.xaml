<Window
    x:Class="WPF_EF_Rebbit_MSTest_Moq.Project.View.NavigationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:WPF_EF_Rebbit_MSTest_Moq.Project.ViewModel"
    Title="Navigation"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <viewModel:NavigationViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button
            Width="70"
            Height="25"
            Margin="20"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Command="{Binding CardSectionOpenCommand}">
            Cards
        </Button>
        <Button
            Grid.Row="1"
            Width="70"
            Height="25"
            Margin="20,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Command="{Binding IndicatorSectionOpenCommand}">
            Indicators
        </Button>
    </Grid>
</Window>
